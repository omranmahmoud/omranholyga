# Environment variable examples
# Use this file as a reference; copy to .env (not committed) for local overrides.

# Base URL for API calls in the frontend. If unset, code falls back to /api.
# In Netlify, /api is proxied to https://omraneva.onrender.com/api via netlify.toml redirect.
VITE_API_BASE=/api

# Show push notification test buttons (dev only)
VITE_SHOW_PUSH_UI=false
