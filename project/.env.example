# Environment variable examples
# Use this file as a reference; copy to .env (not committed) for local overrides.

# Base URL for API calls in the frontend. If unset, code falls back to /api.
# In Netlify, /api is proxied to https://omraneva.onrender.com/api via netlify.toml redirect.
VITE_API_BASE=/api

# Show push notification test buttons (dev only)
VITE_SHOW_PUSH_UI=false

# --- Server Side Secrets (DO NOT commit real values) ---
# These belong in server/.env or in hosting provider environment settings.
MONGODB_URI=
JWT_SECRET=
PORT=5000

# Cloudinary (only expose unsigned preset + cloud name on client; keep API secret private)
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_API_KEY=
VITE_CLOUDINARY_UPLOAD_PRESET=
# NEVER expose this in a committed .env – left blank intentionally
VITE_CLOUDINARY_API_SECRET=
VITE_CLOUDINARY_SECURE=true
VITE_CLOUDINARY_URL=

# Optional explicit API base (e.g. https://omraneva.onrender.com/api)
# Leave blank to allow auto-detection on Netlify.
VITE_API_BASE=
